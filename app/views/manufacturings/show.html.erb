<%- model_class = Manufacturing -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <%= render partial: 'shared/common_show', locals: { model_class: model_class, model: @manufacturing } %>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              manufacturings_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_manufacturing_path(@manufacturing), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              manufacturing_path(@manufacturing),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
