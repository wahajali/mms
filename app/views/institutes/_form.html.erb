<%= bootstrap_form_for @institute, :html => { :class => "form-horizontal institute" } do |f| %>

    <% if @institute.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@institute.errors.count, "error") %> prohibited this institute from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @institute.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <%= render partial: 'shared/shared_partials', locals: { common_data: @common_data, f: f, address: @address, phone_number: @phone_numbers } %>

  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            institutes_path, :class => 'btn btn-default' %>

<% end %>
