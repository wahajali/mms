<%= f.fields_for :phone_numbers do |builder| %>
  <%= builder.select(:number_type, [["Mobile", "Mobile"], ["Landline", "Landline"]]) %>
  <%= builder.text_field :number, placeholder: 'Phone Number' %>
<% end %>
