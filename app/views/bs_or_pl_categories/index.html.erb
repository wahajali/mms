<%- model_class = BsOrPlCategory -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:name) %></th>
      <th><%= model_class.human_attribute_name(:bl_or_pl) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @bs_or_pl_categories.each do |bs_or_pl_category| %>
      <tr>
        <td><%= link_to bs_or_pl_category.id, bs_or_pl_category_path(bs_or_pl_category) %></td>
        <td><%= bs_or_pl_category.name %></td>
        <td><%= bs_or_pl_category.bl_or_pl %></td>
        <td><%=l bs_or_pl_category.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_bs_or_pl_category_path(bs_or_pl_category), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      bs_or_pl_category_path(bs_or_pl_category),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_bs_or_pl_category_path,
            :class => 'btn btn-primary' %>
