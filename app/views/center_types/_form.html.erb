<%= bootstrap_form_for @center_type, :html => { :class => "form-horizontal center_type" } do |f| %>

    <% if @center_type.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@center_type.errors.count, "error") %> prohibited this center_type from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @center_type.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <%= f.text_field :name %>

  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            center_types_path, :class => 'btn btn-default' %>

<% end %>
